#include <bits/stdc++.h>
using namespace std;

int main()
{
    string name("Muthu selvi");
    cout<<"\n String Data : ";
    for(auto it = name.begin(); it != name.end(); it++){
        cout<<*it<<" ";
    }
    return 0;
}
